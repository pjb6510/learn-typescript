// @ts-check

/**
 *
 * @param {Number} a first number
 * @param {Number} b second number
 * @returns {Number}
 */

const sum = (a, b) => {
  return a + b;
};

// const num = sum(5, "10"); // warning
